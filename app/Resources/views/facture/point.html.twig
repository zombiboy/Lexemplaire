{% extends 'base.html.twig' %}

{% block body %}
    <h1>Point</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="OK" />
    {{ form_end(form) }}

{% endblock %}
