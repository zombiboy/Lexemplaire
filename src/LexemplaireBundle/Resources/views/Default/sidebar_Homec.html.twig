{% block sidebar %}

<ul id="menu-genres" class="nav nav-list">
<p><a href="{{ path('commande_new') }}" class="btn btn-primary btn-lg">VENTE</a></p>

 {% for article in articles %}
    <li><a href="{{ path('article_show', { 'id': article.id }) }}">{{ article.libelle }} <span class="badge badge-info">{{ article.stock }}</span></a></li>
 {% endfor %}
</ul>    

{% endblock %}

